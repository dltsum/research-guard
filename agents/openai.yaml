interface:
  display_name: "Research Guard"
  short_description: "Macro-first, instruction-bound research and constructive numerical audit"
  default_prompt: "Use $research-guard to lift local observations into a macro-first paper spine, register every multistep requirement, enforce evidence gates, and construct protocol-legal numerical intervals and jointly feasible anchors."
policy:
  allow_implicit_invocation: true
